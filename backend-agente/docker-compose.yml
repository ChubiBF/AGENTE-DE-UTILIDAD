version: '3.8'

services:
  backend-agente:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agente_cognitivo_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: always